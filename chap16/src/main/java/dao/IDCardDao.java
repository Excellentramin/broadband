/*
 * package dao;
 * 
 * import org.apache.ibatis.annotations.Select;
 * 
 * import pojo.IDCard;
 * 
 * //����ʹ��ע�ⷽʽ public interface IDCardDao {
 * 
 * @Select("select id as cid ,code from idcard where id=#{cardid}") public
 * IDCard getCard(int cardid); }
 */